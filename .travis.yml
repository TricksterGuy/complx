sudo: required
dist: focal
language: cpp
jobs:
  include:
    - os: linux
      compiler: gcc
    - os: linux
      compiler: clang
before_install:
  - apt update -qq
  - apt install -y libboost-test-dev libboost-serialization-dev
script:
  - ./configure test
  - ./configure test
  - cd build-test
  - sudo make install
  - sudo ldconfig
  - make test