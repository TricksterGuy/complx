set(SRC_LOGGING
    logger.cpp
)

add_library(
    logger
    SHARED
    ${SRC_LOGGING}
)

install(TARGETS logger LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR} RUNTIME DESTINATION ${CMAKE_INSTALL_LIBDIR})
